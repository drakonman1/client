import React from "react";
import SettingsCard from "./SettingsCard";

const UserManagement = () => {
    return (
        <SettingsCard title="User Management">
            <p>Manage employees, permissions, and roles (coming soon).</p>
        </SettingsCard>
    );
};

export default UserManagement;